#include <stdio.h>
void sorted(int N,int arr[]){
    int arr[];
    for(int i=0;i<=N;i++){
        if(arr(i) > arr(i+1)){
            printf("Sorted");
        }
        else {
            printf("Unsorted");
        }
    }
}
int main(){
    int N;
    int arr[];
    scanf("%d",&N);
    for(int i=0;i<=N;i++){
        printf("%d ",arr[i]);
    }
    printf("%d",sorted(N,arr[]));
    return 0;
}